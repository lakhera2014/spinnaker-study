kind: Deployment
apiVersion: apps/v1
metadata:
    labels:
        app: prashant-solo-microservices
        stack: frontend
        source: demo
    name: prashant-solo-microservices
    namespace: microservices
spec:
    replicas: 3
    selector:
        matchLabels:
            run: prashant-solo-microservices
    template:
        metadata:
            labels:
                app: prashant-solo-microservices
                run: prashant-solo-microservices
                source: demo
        spec:
            containers:
                -   image: 219099013464.dkr.ecr.us-west-2.amazonaws.com/prashant-solo-microservices
                    name: prashant-solo-microservices
                    ports:
                        -   name: web
                            containerPort: 8080
---

kind: Service
apiVersion: v1
metadata:
    labels:
        app: prashant-solo-microservices-service
    name: prashant-solo-microservices-service
    namespace: microservices
spec:
    selector:
        source: demo
        app: prashant-solo-microservices
    ports:
        -   name: web
            protocol: TCP
            port: 30201
            targetPort: 8080

---

apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
    annotations:
        nginx.ingress.kubernetes.io/rewrite-target: /$2
    name: mild-temper-ingress
    namespace: microservices
spec:
    rules:
        -   http:
                paths:
                    -   path: /mild-temper(/|$)(.*)
                        backend:
                            serviceName: prashant-solo-microservices-service
                            servicePort: 30201


